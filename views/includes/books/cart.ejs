<!DOCTYPE html>
<html>
<% include ./../../partials/head %>

  <body>
    <% include ./../../navigation %>
      <div class="container-fluid">
        <%if(books.length == 0){%>
          <section class="text-center">
            <h1 class="display-4 py-5">Your Cart is empty!</h1>
          </section>
          <%}else{%>
            <section class="text-center">
              <h1 class="jumbotron-heading">Books Cart</h1>
              <p class="lead text-muted">Below are the list of Books available in your cart</p>
            </section>
            <%}%>
      </div>
      <div class="album py-5">
        <div class="container">
          <div class="row">
            <% if(books.length > 0)for(var i=0; i<books.length; i++) {%>
              <div class="col-md-3">
                <div class="card mb-4 box-shadow">
                  <a href="/book/show/<%=books[i]._id%>">
                    <img class="card-img-top" src="<%= books[i].image_path%>" alt="Card image cap">
                  </a>
                  <div class="card-body">
                    <h5 class="card-title">
                      <%= books[i].title%>
                    </h5>
                    <p class="card-text"><%=books[i].desc%></p>
                    <p class="card-text">                    By: <i><%=books[i].author%></i><br>
                      ISBN: <i><%=books[i].isbn%></i><br>
                      Category: <i><%=books[i].category%></i><br><br>
                    </p>
                    <div class="d-flex justify-content-between align-items-center">
                      <div class="btn-group" id="link-holder">
                        <a href="/book/show/<%=books[i]._id%>">
                          <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                        </a>
                        <a onclick="removeFromCart()" href="/cart/remove/<%=books[i]._id%>">
                          <button type="button" class="btn btn-sm btn-outline-secondary">Remove</button>
                        </a>
                      </div>
                      <small class="text-muted"> $
                        <%= books[i].price%>
                      </small>
                    </div>
                  </div>
                </div>
              </div>
              <% } %>
          </div>
        </div>
      </div>
      <% include ./../../partials/script_includes %>
  </body>

</html>